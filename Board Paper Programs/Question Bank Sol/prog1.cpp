#include <iostream> //Find max of two numbers using if and else
using namespace std;
int main(){
    int a, b, max;
    cout << "Enter two numbers: \n";
    cin >> a >> b; 
    if(a>b)
    {
        cout << " Max is: " << a;
    }
    else
    {
        cout << " Max is: " << b;
    }
    
}
